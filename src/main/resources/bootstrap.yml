encrypt:
  keyStore:
    location: classpath:/server.jks
    password: letmein
    alias: mytestkey
    secret: changeme

spring:
  cloud:
    config:
      server:
        encrypt:
          enabled: false